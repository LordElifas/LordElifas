let initialState = {
    friendsList: [
        {id: 1, name: "Andrew"},
        {id: 2, name: "Sasha"},
        {id: 3, name: "Sveta"}
    ]
}
const friendsListReducer = (state= initialState, action) => {

    return  state;
}

export default friendsListReducer;